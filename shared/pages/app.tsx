import React, { ReactElement } from 'react'
import { Header } from 'react-common'

export default function App(): ReactElement {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  )
}